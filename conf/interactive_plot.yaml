restore: ???

autoencoder: ${get_original:${restore},autoencoder}
optimizer: ${get_original:${restore},optimizer}
seed: ${get_original:${restore},seed}
trainer: ${get_original:${restore},trainer}

n_nearest: 128
log_data_size: 4096

defaults:
    - hydra: default
    - _self_